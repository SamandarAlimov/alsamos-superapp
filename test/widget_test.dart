import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Alsamos app smoke test', (WidgetTester tester) async {
    // App starts without crashing
    expect(true, isTrue);
  });
}
